# ZulipChat MCP - Environment Configuration Template
#
# Copy this file to .env for local development
# For production, pass credentials via CLI arguments or MCP client configuration
#
# ============================================================================
# MANDATORY PARAMETERS - Required for basic functionality
# ============================================================================

# Your Zulip user credentials (REQUIRED)
ZULIP_EMAIL=user@your-org.zulipchat.com
ZULIP_API_KEY=your-api-key-here
ZULIP_SITE=https://your-org.zulipchat.com

# ============================================================================
# OPTIONAL PARAMETERS - Enable advanced AI agent features
# ============================================================================

# Bot identity for AI agent capabilities (OPTIONAL)
# Enables: agent_message, request_user_input, task tracking, AFK mode
# Leave blank to use user account only
ZULIP_BOT_EMAIL=
ZULIP_BOT_API_KEY=
ZULIP_BOT_NAME=
ZULIP_BOT_AVATAR_URL=

# === Development Settings ===
# Enable debug logging
MCP_DEBUG=false

# Enable background message listener service (optional)
ENABLE_LISTENER=false

# Database location (optional, uses default if not set)
#DATABASE_PATH=~/.local/share/zulipchat-mcp/data.db

# ============================================================================
# HOW TO GET API CREDENTIALS
# ============================================================================
#
# User API Key (MANDATORY):
# 1. Go to https://your-org.zulipchat.com/#settings/api-keys
# 2. Click "Generate new API key"
# 3. Copy the key to ZULIP_API_KEY above
#
# Bot API Key (OPTIONAL - for advanced features):
# 1. Go to https://your-org.zulipchat.com/#settings/your-bots
# 2. Create a new bot or use existing
# 3. Click "Generate API key" for the bot
# 4. Copy credentials to ZULIP_BOT_* fields above
#
# ============================================================================
# INSTALLATION METHODS
# ============================================================================
#
# Quick Start (GitHub):
#   uvx --from git+https://github.com/akougkas/zulipchat-mcp.git zulipchat-mcp \
#     --zulip-email user@org.com \
#     --zulip-api-key YOUR_KEY \
#     --zulip-site https://org.zulipchat.com
#
# With Bot (Advanced):
#   Add: --zulip-bot-email bot@org.com --zulip-bot-api-key BOT_KEY
#
# For Claude Code:
#   claude mcp add zulipchat \
#     -e ZULIP_EMAIL=user@org.com \
#     -e ZULIP_API_KEY=YOUR_KEY \
#     -e ZULIP_SITE=https://org.zulipchat.com \
#     -- uvx --from git+https://github.com/akougkas/zulipchat-mcp.git zulipchat-mcp

# Notification System
# Set ZULIP_DEV_NOTIFY=1 environment variable to override AFK gating for development
#ZULIP_DEV_NOTIFY=1